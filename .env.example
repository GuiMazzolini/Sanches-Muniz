# Production site URL (no trailing slash). Used for canonical URLs, Open Graph, and sitemap.
VITE_SITE_URL=https://www.sanchesemuniz.com

# Analytics (optional). When true, shows cookie banner and loads Plausible after consent.
VITE_ANALYTICS_ENABLED=false
VITE_PLAUSIBLE_DOMAIN=www.sanchesemuniz.com
